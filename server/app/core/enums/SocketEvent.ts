export enum SocketEvent {
    MESSAGE = 'message',
    CONNECTION = 'connection',
    CLOSE = 'close',
    ERROR = 'error',
    START = 'START',
    WAIT = 'WAIT',
    JOIN = 'JOIN',
    STOP = 'STOP',
    PAUSE = 'PAUSE',
    ACTION = "ACTION"
  }